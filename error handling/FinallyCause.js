// to run this file after navigate in terminal write node FinallyCause.js
const f = () => {
  try {
    let a = 0;
    console.log(program);
    console.log("inside try block");
  } catch (error) {
    console.log("inside catch block");
    console.log(p);
  } finally {
    console.log("this is finally block always run");
    // user for like
    // close the File
    // exit to loop
    // write default value
    // also run if error not occure 
    // also run if return something form try
  }
};
f();