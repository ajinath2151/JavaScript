
// merge two string into one

// const mergeFuncitn = (str1, str2) => {
//     let strResult = str1 + str2;
//     console.log(strResult);
// }
// mergeFuncitn("ajinath","nobody");

const mergeFuncitn = (str1, str2) => {
    let strResult = str1 + str2;
    return strResult;
}
let result = mergeFuncitn("ajinath","nobody");
console.log(result);

